#include "Integration.hpp"

using namespace mtu;

void Integration (std::vector<double> input, 
    double a,   // a, b: 積分区間
    double b,  
    int n,      // 分割数
    std::vector<double>& output)
{
    double h = (b - a) / n;
    for (int i = 0; i < n; i++) {
        
    }
}